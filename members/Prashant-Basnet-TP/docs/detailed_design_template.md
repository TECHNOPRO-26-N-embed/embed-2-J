# 詳細設計書 — 組込み開発実習

バシネット　プラサント・2026-05-22 / グループ：2-J

---

## 0. 基本設計書との接続確認

| 項目 | basic_design.md から転記 |
|:--|:--|
| 作品タイトル | 電子サイコロ |
| 状態の種類（1-2 状態遷移から） | 初期化、待機、回転、結果 |
| 実装する関数の数（2-2 関数一覧から） | 6個（setup, loop, rollTheDice, showNumber, one〜six）|
| グローバル変数の合計バイト数（2-1 SRAM確認から） | 約30B（目安）|

---

## 1. グローバル変数・定数の設計

```
【ピン定義】
  aPin = 7
  bPin = 8
  cPin = 2
  dPin = 3
  ePin = 4
  fPin = 6
  gPin = 5
  PIN_BUTTON = A0
  PIN_BUZZER = 10
  PIN_CHAOS = A5

【状態管理】
  currentState : int = 0   // 0:待機 1:回転 2:結果

【タイマー（millis()用）】
  lastDebounceTime : unsigned long = 0
  debounceDelay : const int = 200
  lastAnimationTime : unsigned long = 0
  animationInterval : const int = 100

【センサー・入力値】
  buttonPressed : bool = false

【その他のフラグ・カウンター】
  animationStep : int = 0
  BEEP_FREQUENCY : const int = 3000
```

---

## 2. 各関数の詳細設計

### setup() — 初期化処理

```
【処理の流れ】
1. 7セグLED各ピンをOUTPUTに設定
2. ボタンピンをINPUT_PULLUPに設定
3. ブザーピンをOUTPUTに設定
4. randomSeed(analogRead(PIN_CHAOS))で乱数初期化
5. 起動アニメーション（1〜6を順番に表示＋ブザー音）
```

---

### loop() — メインループ

```
【処理の流れ】
＜毎ループ実行すること＞
- 現在時刻を取得: now = millis()
- ボタン状態をdigitalReadで取得し、チャタリング対策（millisで判定）

＜currentStateが0（待機）のとき＞
- ボタンが押されたらcurrentState=1にしてrollTheDice()を呼ぶ

＜currentStateが1（回転）のとき＞
- アニメーションをmillis()で管理し、10回ランダム表示
- 終了したらcurrentState=2にして結果表示

＜currentStateが2（結果）のとき＞
- 最終結果を表示し、ボタンが離されたらcurrentState=0に戻す
```

---

### rollTheDice() — サイコロを振る処理

```
【処理の流れ】
1. animationStep=0にリセット
2. millis()で100msごとにランダムな数字を表示（10回）
3. 各回で短いブザー音を鳴らす
4. 最後に本当のサイコロ結果を表示し、長めのブザー音
5. currentState=2にする
```

---

### showNumber(x) — 数字を7セグLEDに表示

```
【処理の流れ】
1. xの値に応じてone()〜six()を呼ぶ
```

---

### one()〜six() — 各数字の7セグLED制御

```
【処理の流れ】
1. 各セグメントピンをON/OFFで制御し、数字を表示
```

---

## 3. 重要ロジックの詳細設計

### 3-1. チャタリング防止（デバウンス処理）

```
【処理の流れ】
1. ボタンが押されたら、lastDebounceTimeと現在時刻を比較
2. 一定時間（debounceDelay=200ms）経過していれば有効な押下とする
3. ボタンが離されるまで次の入力を受け付けない
```

---

### 3-2. millis() を使ったタイマー管理

```
【処理の流れ】
1. アニメーションやボタン判定は、delay()を使わずmillis()で管理
2. now - lastAnimationTime >= animationIntervalなら次のアニメーションへ
```

---

## 4. デバッグ出力計画（任意）

| No | 確認したい内容 | 挿入する関数 | Serial.println の内容例 |
|:---|:---|:---|:---|
| 1 | ボタンが押されたか | loop() | Serial.println("button pressed"); |
| 2 | 状態遷移 | loop() | Serial.println(currentState); |
| 3 | アニメーション進行 | rollTheDice() | Serial.println(animationStep); |

---

## 5. 単体テスト仕様書

### 5-1. 入力系テスト

| No | テスト対象の関数 | 入力・操作 | 期待する結果 | 実際の結果 | 合否 |
|:---|:---|:---|:---|:---|:---|
| 1 | loop() | ボタンを1回押す | サイコロが1回だけ振られる | | [ ] |
| 2 | loop() | ボタンを連打 | 1回ごとに1回だけ振られる | | [ ] |

### 5-2. 出力系テスト

| No | テスト対象の関数 | 入力・操作 | 期待する結果 | 実際の結果 | 合否 |
|:---|:---|:---|:---|:---|:---|
| 1 | showNumber() | 1〜6を表示 | 7セグLEDが正しく点灯 | | [ ] |
| 2 | rollTheDice() | サイコロを振る | ブザー音が鳴る | | [ ] |

### 5-3. タイミング・並行動作テスト

| No | テスト内容 | テスト手順 | 期待する結果 | 実際の結果 | 合否 |
|:---|:---|:---|:---|:---|:---|
| 1 | アニメーション中のボタン反応 | アニメ中にボタンを押す | 追加で振られない | | [ ] |
| 2 | millis()タイマー精度 | アニメーション間隔を測定 | 設計通りの間隔 | | [ ] |

---

## 6. AIレビュー記録

### Q1: 実装上の問題確認

**AIの回答（要約）：**
- ピンや変数の型に問題なし
- millis()でタイミング管理しているので応答性も良い
- チャタリング対策も十分

**対応した内容：**
- 変数名・型・ピン番号を再確認
- millis()で全体のタイミングを管理

---

### Q2: 単体テスト仕様の確認

**AIの回答（要約）：**
- 各関数のテストが網羅されている
- 境界値（1,6）や連打時の動作もテストされている

**対応した内容：**
- テスト項目を追加し、抜けがないか確認

---

## 7. グループレビュー記録

### 7-1. 指摘一覧

| No | 指摘内容 | 指摘者 | 対応 |
|:---|:---|:---|:---|
| 1 |  |  |  |
| 2 |  |  |  |
| 3 |  |  |  |

### 7-2. レビューを受けて変更した点

-
-

---

*初版: 2026-05-22 / AIレビュー: 2026-05-22 / グループレビュー後更新: 2026-05-22*
